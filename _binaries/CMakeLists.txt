# CMakeLists.txt for _binaries
# Add subdirectories for all relevant binary components
add_subdirectory(abbs)
# add_subdirectory(demos)  # Disabled to remove demos from build
add_subdirectory(services)
add_subdirectory(examples)
